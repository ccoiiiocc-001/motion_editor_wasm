--- a/js/retrofx.js
+++ b/js/retrofx.js
@@ -1048,7 +1048,11 @@
         const savedCaching = obj.objectCaching;
         obj.objectCaching = false;
         obj.stroke = '#ffffff';
-        obj.fill = obj.type === 'path' ? '' : '#ffffff';
+        if (obj.type === 'path') {
+            obj.fill = obj.fill ? '#ffffff' : '';
+        } else {
+            obj.fill = '#ffffff';
+        }
         obj.opacity = 1;
         ctx.save();
         obj.render(ctx);
