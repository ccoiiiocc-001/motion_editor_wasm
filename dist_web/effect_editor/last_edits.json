[
  {
    "name": "multi_replace_file_content",
    "args": {
      "Description": "\"8\ubc88 \ubcbd \uad6c\uba4d \ud22c\uc2dc \ud6a8\uacfc\ub97c 8\ubc29\ud5a5 \uc2ac\ub77c\uc774\ub354\uc640 3D \ubcbd\ub3cc \ucc3d\ud2c0\ub85c \ubcf5\uad6c\"",
      "Instruction": "\"HolePerspective \ud544\ud130\uc640 applyRetroEffect8 \ud568\uc218 \uad50\uccb4\"",
      "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":481,\"ReplacementContent\":\"fabric.Image.filters.HolePerspective = fabric.util.createClass(fabric.Image.filters.BaseFilter, {\\n    type: 'HolePerspective',\\n    thickness: 40,\\n    angle: 0,\\n    maskObj: null,\\n    applyTo: function(options) {\\n        const data = options.imageData.data;\\n        const w = options.imageData.width;\\n        const h = options.imageData.height;\\n        const copyData = new Uint8ClampedArray(data);\\n        \\n        const rad = this.angle * Math.PI / 180;\\n        const dxDepth = Math.cos(rad) * this.thickness;\\n        const dyDepth = Math.sin(rad) * this.thickness;\\n\\n        const brickL = 30; // \ubcbd\ub3cc \uae38\uc774\\n        const brickH = 10; // \ubcbd\ub3cc \ub192\uc774\\n\\n        for (let y = 0; y < h; y++) {\\n            for (let x = 0; x < w; x++) {\\n                const dstIdx = (y * w + x) * 4;\\n                const weight = this.maskObj ? getMaskWeightAt(this.maskObj, x, y, w, h) : 0;\\n                \\n                if (weight > 0.5) {\\n                    // \uad6c\uba4d \ub0b4\ubd80\\n                    // \ub808\uc774\uce90\uc2a4\ud305\uc744 \ud1b5\ud574 \ubcbd\uba74 \ud14d\uc2a4\ucc98(\ub450\uaed8) \ub80c\ub354\ub9c1\\n                    let hit = false;\\n                    let hitX = x;\\n                    let hitY = y;\\n                    let hitT = 0; // 0 (\uc548\ucabd) ~ 1 (\ubc14\uae65\ucabd)\\n                    \\n                    const steps = Math.ceil(this.thickness);\\n                    for (let step = 1; step <= steps; step++) {\\n                        const t = step / steps;\\n                        const rayX = x + dxDepth * t;\\n                        const rayY = y + dyDepth * t;\\n                        if (rayX >= 0 && rayX < w && rayY >= 0 && rayY < h) {\\n                            const rayWeight = getMaskWeightAt(this.maskObj, rayX, rayY, w, h);\\n                            if (rayWeight < 0.5) {\\n                                // \ubcbd(\uad6c\uba4d \ubc14\uae65)\uc744 \ub9cc\ub0a8\\n                                hit = true;\\n                                hitX = rayX;\\n                                hitY = rayY;\\n\n<truncated 9427 bytes>",
      "TargetFile": "\"d:\\\\myData\\\\home_page\\\\keyword_tables\\\\motion_editor\\\\effect_editor\\\\js\\\\retrofx.js\"",
      "toolAction": "\"Restoring HolePerspective\"",
      "toolSummary": "\"Restoring HolePerspective to final state\""
    }
  },
  {
    "name": "multi_replace_file_content",
    "args": {
      "Description": "\"\uc774\ubbf8\uc9c0 \ucc22\uae30 \ud544\ud130\uc640 \ucee8\ud2b8\ub864 \ud328\ub110\uc744 \ub2e8\uc21c\ud654\ub41c \ubc84\uc804\uc73c\ub85c \ubcf5\uad6c\"",
      "Instruction": "\"ImageTear \ud544\ud130\uc640 applyRetroEffect6 \ud568\uc218 \uad50\uccb4\"",
      "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":381,\"ReplacementContent\":\"fabric.Image.filters.ImageTear = fabric.util.createClass(fabric.Image.filters.BaseFilter, {\\n    type: 'ImageTear',\\n    jagged: 10,\\n    maskObj: null,\\n    applyTo: function (options) {\\n        const data = options.imageData.data;\\n        const w = options.imageData.width;\\n        const h = options.imageData.height;\\n        const copyData = new Uint8ClampedArray(data);\\n\\n        // \ube60\ub978 \ub178\uc774\uc988 \ub9f5 \uc0dd\uc131\\n        const noise = new Float32Array(w * h);\\n        for (let y = 0; y < h; y += 4) {\\n            for (let x = 0; x < w; x += 4) {\\n                const n = (Math.random() - 0.5) * this.jagged;\\n                for (let dy = 0; dy < 4 && y + dy < h; dy++) {\\n                    for (let dx = 0; dx < 4 && x + dx < w; dx++) {\\n                        noise[(y + dy) * w + (x + dx)] = n;\\n                    }\\n                }\\n            }\\n        }\\n\\n        for (let y = 0; y < h; y++) {\\n            for (let x = 0; x < w; x++) {\\n                const dstIdx = (y * w + x) * 4;\\n                \\n                // \ud53d\uc140\uc758 \uc6d0\ubcf8 \uc704\uce58\uc5d0 \ub178\uc774\uc988\ub97c \ub354\ud574 \ub9c8\uc2a4\ud06c \ucc38\uc870 \uc704\uce58 \uacc4\uc0b0\\n                const n = noise[y * w + x];\\n                const nx = Math.max(0, Math.min(w - 1, x + n));\\n                const ny = Math.max(0, Math.min(h - 1, y + n));\\n                \\n                const weight = this.maskObj ? getMaskWeightAt(this.maskObj, nx, ny, w, h) : 0;\\n\\n                if (weight > 0.5) {\\n                    data[dstIdx] = 0;\\n                    data[dstIdx + 1] = 0;\\n                    data[dstIdx + 2] = 0;\\n                    data[dstIdx + 3] = 0;\\n                } else {\\n                    data[dstIdx] = copyData[dstIdx];\\n                    data[dstIdx + 1] = copyData[dstIdx + 1];\\n                    data[dstIdx + 2] = copyData[dstIdx + 2];\\n                    data[dstIdx + 3] = copyData[dstIdx + 3];\\n                }\\n            }\\n        }\\n    }\\n});\",\"StartLine\":272,\"TargetContent\":\"fabri\n<truncated 10286 bytes>",
      "TargetFile": "\"d:\\\\myData\\\\home_page\\\\keyword_tables\\\\motion_editor\\\\effect_editor\\\\js\\\\retrofx.js\"",
      "toolAction": "\"Restoring ImageTear\"",
      "toolSummary": "\"Restoring ImageTear filter to final state\""
    }
  },
  {
    "name": "multi_replace_file_content",
    "args": {
      "Description": "\"\ud37c\uc990 \uc870\uac01\ud654 \ud544\ud130\uc640 \ucee8\ud2b8\ub864 \ud328\ub110\uc744 \ucd5c\uc885 3D \ubca0\ubca8\uc774 \uc801\uc6a9\ub41c \uc0c1\ud0dc\ub85c \ubcf5\uad6c\"",
      "Instruction": "\"PuzzleShuffle \ud544\ud130\uc640 applyRetroEffect9 \ud568\uc218 \uad50\uccb4\"",
      "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":502,\"ReplacementContent\":\"fabric.Image.filters.PuzzleShuffle = fabric.util.createClass(fabric.Image.filters.BaseFilter, {\\n    type: 'PuzzleShuffle',\\n    cols: 6,\\n    rows: 4,\\n    maskObj: null,\\n    applyTo: function(options) {\\n        const data = options.imageData.data;\\n        const w = options.imageData.width;\\n        const h = options.imageData.height;\\n        const copyData = new Uint8ClampedArray(data);\\n        const cols = Math.max(2, Math.round(this.cols));\\n        const rows = Math.max(2, Math.round(this.rows));\\n        const cellW = w / cols;\\n        const cellH = h / rows;\\n        \\n        // \ud0ed(\uc870\uac01 \uc774\uc74c\ub9e4) \uc0dd\uc131 \ud568\uc218\\n        const getTab = (edgeI, edgeJ, t, seed) => {\\n            const hash = Math.sin(edgeI * 12.9898 + edgeJ * 78.233 + seed) * 43758.5453;\\n            const dir = (hash - Math.floor(hash)) > 0.5 ? 1 : -1;\\n            const bump = Math.exp(- (t * t * 45)); \\n            return dir * bump; \\n        };\\n\\n        const pieceStates = [];\\n        for (let j = 0; j < rows; j++) {\\n            for (let i = 0; i < cols; i++) {\\n                const cx = (i + 0.5) * cellW;\\n                const cy = (j + 0.5) * cellH;\\n                const weight = this.maskObj ? getMaskWeightAt(this.maskObj, cx, cy, w, h) : 0;\\n                \\n                const hash = (i * 17 + j * 31) % 97;\\n                let state = 0; // 0: \uc815\ubc29\ud5a5, 1: missing, 2: scattered\\n                let rot = 0, ox = 0, oy = 0;\\n                \\n                if (weight > 0.5) {\\n                    if (hash % 4 === 0) {\\n                        state = 1; // missing\\n                    } else {\\n                        state = 2; // scattered\\n                        rot = ((hash % 5) - 2) * 0.15;\\n                        ox = ((hash % 7) - 3) * (cellW * 0.15);\\n                        oy = (((hash * 3) % 7) - 3) * (cellH * 0.15);\\n                    }\\n                }\\n                \\n                pieceStates.push({ i, j, state, rot, ox, oy })\n<truncated 10167 bytes>",
      "TargetFile": "\"d:\\\\myData\\\\home_page\\\\keyword_tables\\\\motion_editor\\\\effect_editor\\\\js\\\\retrofx.js\"",
      "toolAction": "\"Restoring PuzzleShuffle\"",
      "toolSummary": "\"Restoring PuzzleShuffle filter to final gapless state\""
    }
  },
  {
    "name": "multi_replace_file_content",
    "args": {
      "Description": "\"UI \ud544\ud130 \ucee8\ud2b8\ub864 \ubc0f \ucd08\uae30 \uc124\uc815\uac12\uc744 \uc694\uad6c\uc0ac\ud56d\uc5d0 \ub9de\uac8c \uc218\uc815 (\ubcbd \uad6c\uba4d \ub450\uaed8 \ubc29\ud5a5 \uc2ac\ub77c\uc774\ub354 \ucd94\uac00, \ucc22\uae30/\ud37c\uc990 \ub3d9\uc801 \ud6a8\uacfc \uc81c\uac70)\"",
      "Instruction": "\"applyRetroEffect 6, 8, 9\uc758 UI \ucee8\ud2b8\ub864\uc744 \uc218\uc815\ud558\uc5ec \uc694\uad6c\uc0ac\ud56d\uc5d0 \ub9de\ub294 \uc2ac\ub77c\uc774\ub354(\uac70\uce60\uae30, \ubc29\ud5a5 \ub4f1)\ub85c \ubcc0\uacbd\"",
      "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":2018,\"ReplacementContent\":\"        window.globalRetroMemory[id] = {\\n            amount: 0, roughness: 10,\\n            brushSize: 40, feather: 20, maskObj: null\\n        };\\n    }\\n    obj.retroSettings = { ...window.globalRetroMemory[id], activeFxId: id };\\n    let filter = obj.filters.find(f => f.type === 'ImageTear');\\n    if (!filter) {\\n        filter = new fabric.Image.filters.ImageTear({ ...obj.retroSettings });\\n        obj.filters = [filter];\\n    }\\n\\n    const syncFilter = () => {\\n        filter.amount = obj.retroSettings.amount;\\n        filter.roughness = obj.retroSettings.roughness;\\n        filter.maskObj = obj.retroSettings.maskObj;\\n        obj.dirty = true;\\n        obj.applyFilters();\\n        window.canvas.renderAll();\\n    };\\n\\n    window.showFilterControls('\uc774\ubbf8\uc9c0 \ucc22\uae30 (\uc601\uc5ed \uc9c0\uc815)', [\\n        { id: 'roughness', label: '\uac70\uce60\uae30 (\ud1b1\ub2c8)', min: 0, max: 20, step: 1, value: obj.retroSettings.roughness, inputType: 'range' },\\n        { id: 'brushSize', label: '\ud83d\udd8c\ufe0f \ubd93 \ud06c\uae30', min: 5, max: 150, step: 1, value: obj.retroSettings.brushSize },\\n        { id: 'feather', label: '\u2601\ufe0f \ud398\ub354', min: 0, max: 100, step: 1, value: obj.retroSettings.feather }\\n    ], (pid, val) => {\\n        obj.retroSettings[pid] = val;\\n        window.globalRetroMemory[id][pid] = val;\",\"StartLine\":1984,\"TargetContent\":\"        window.globalRetroMemory[id] = {\\n            tearPos: 0.5, jagged: 18, gap: 22, direction: 0, fiber: 0.4,\\n            brushSize: 40, feather: 20, maskObj: null\\n        };\\n    }\\n    obj.retroSettings = { ...window.globalRetroMemory[id], activeFxId: id };\\n    let filter = obj.filters.find(f => f.type === 'ImageTear');\\n    if (!filter) {\\n        filter = new fabric.Image.filters.ImageTear({ ...obj.retroSettings, seed: 1.37 });\\n        obj.filters = [filter];\\n    }\\n\\n    const syncFilter = () => {\\n        filter.tearPos = obj.retroSettings.tearPos;\\n        filter.jagged = obj.retroSettings.jagged;\\n        filter.gap = obj.retroSettings\n<truncated 8408 bytes>",
      "TargetFile": "\"d:\\\\myData\\\\home_page\\\\keyword_tables\\\\motion_editor\\\\effect_editor\\\\js\\\\retrofx.js\"",
      "toolAction": "\"Fixing UI controls\"",
      "toolSummary": "\"Replacing UI controls for effects 6, 8, 9\""
    }
  },
  {
    "name": "multi_replace_file_content",
    "args": {
      "Description": "\"Fix applyRetroEffect7 based on user requirements\"",
      "Instruction": "\"Restore line drawing logic for PaintDrip effect\"",
      "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":640,\"ReplacementContent\":\"fabric.Image.filters.PaintDrip = fabric.util.createClass(fabric.Image.filters.BaseFilter, {\\n    type: 'PaintDrip',\\n    amount: 0,\\n    dripLen: 120,\\n    viscosity: 0.6,\\n    maskObj: null,\\n    applyTo: function(options) {\\n        const data = options.imageData.data;\\n        const w = options.imageData.width;\\n        const h = options.imageData.height;\\n        const copyData = new Uint8ClampedArray(data);\\n        const amt = Math.max(0, Math.min(1, this.amount));\\n        const maxDrip = amt * this.dripLen;\\n        const visc = Math.max(0.1, this.viscosity);\\n\\n        for (let y = 0; y < h; y++) {\\n            const yNorm = y / h;\\n            for (let x = 0; x < w; x++) {\\n                const dstIdx = (y * w + x) * 4;\\n                const weight = getMaskWeightAt(this.maskObj, x, y, w, h);\\n                let sx = x;\\n                let sy = y;\\n                if (weight > 0.001 && maxDrip > 0.5) {\\n                    const drip = maxDrip * Math.pow(Math.max(0, yNorm - 0.08), visc);\\n                    sy = y - drip;\\n                    const px = sampleBilinear(copyData, w, h, sx, sy);\\n                    data[dstIdx] = px[0] * weight + copyData[dstIdx] * (1 - weight);\\n                    data[dstIdx + 1] = px[1] * weight + copyData[dstIdx + 1] * (1 - weight);\\n                    data[dstIdx + 2] = px[2] * weight + copyData[dstIdx + 2] * (1 - weight);\\n                    data[dstIdx + 3] = px[3] * weight + copyData[dstIdx + 3] * (1 - weight);\\n                } else {\\n                    data[dstIdx] = copyData[dstIdx];\\n                    data[dstIdx + 1] = copyData[dstIdx + 1];\\n                    data[dstIdx + 2] = copyData[dstIdx + 2];\\n                    data[dstIdx + 3] = copyData[dstIdx + 3];\\n                }\\n            }\\n        }\\n    }\\n});\",\"StartLine\":602,\"TargetContent\":\"fabric.Image.filters.PaintDrip = fabric.util.createClass(fabric.Image.filters.BaseFilter, {\\n    type: 'PaintDrip',\\n    amount: 0,\\n    d\n<truncated 10949 bytes>",
      "TargetFile": "\"d:\\\\myData\\\\home_page\\\\keyword_tables\\\\motion_editor\\\\effect_editor\\\\js\\\\retrofx.js\"",
      "toolAction": "\"Replacing file content\"",
      "toolSummary": "\"Editing retrofx.js\""
    }
  }
]